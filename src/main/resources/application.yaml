spring:
  application:
    name: todo-one
  datasource:
    url: jdbc:mysql://mysql:3306/todo?createDatabaseIfNotExist=true
    username: root
    password: ali45
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 8081
